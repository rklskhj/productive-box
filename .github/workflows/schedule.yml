name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{secrets.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}}
          GIST_ID: ${{ secrets.rklskhj / gist:eed9ae9b30a69ecccfabe98248f2f39c }}
          TIMEZONE: Asia/Seoul
